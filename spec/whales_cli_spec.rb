require 'spec_helper'

describe WhalesCli do
  it 'has a version number' do
    expect(WhalesCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
